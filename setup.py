from setuptools import setup, find_packages

setup(name='mprofile',
      version='0.1',
      description='Mutation calling and translocation mapping or targeted sequencing data',
      url='https://github.com/aldob/mProfile',
      packages=find_packages(),
      author='Aldo Saleh Bader',
      zip_safe=False)
